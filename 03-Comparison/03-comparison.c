#include <stdio.h>

int main(void)
{
    int x = 3;
    int y = 2;
    if(x > y)
    {
        printf("%d is greater than %d", x, y);
    }
    else
    {
        printf("%d is greater than %d", y, x);
    }
    
}